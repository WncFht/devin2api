# tailscaled 每次(重)启动都会把 jump ts-input 重插到 ip filter INPUT 顶部,
# 把 gwcap 的 bypass 规则压到 jump 之后 -> gwcap un-NAT 回包被 ts-input 反欺骗
# drop,所有出向 :3003 连接挂死。tailscaled 启动后重插一次 bypass。
[Service]
ExecStartPost=-/usr/local/libexec/gwcap/ensure-bypass.sh
