name: Security

on:
  push:
    branches: [main]
  pull_request:
  # 每周一扫一遍：Go 漏洞库持续更新，已发布的依赖会后知后觉中招；
  # 错峰分钟，不跟其他仓库的整点 cron 挤同一时刻。
  schedule:
    - cron: '17 3 * * 1'

permissions:
  contents: read

jobs:
  govulncheck:
    name: govulncheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: ./.github/actions/setup-go

      - name: govulncheck
        # 钉版本：@latest 让 CI 结果随上游发版漂移（同一提交隔天可红可绿）；
        # 漏洞库时效性由 DB 侧保证，工具版本只求可复现。
        run: go run golang.org/x/vuln/cmd/govulncheck@v1.8.0 ./...
